apiVersion: v1
kind: Pod
metadata:
  name: cpu-stress
  namespace: recovery
spec:
  containers:
  - name: stress
    image: busybox:1.36
    command: ["sh", "-c", "yes > /dev/null"]
    resources:
      requests:
        cpu: "100m"
      limits:
        cpu: "200m"
